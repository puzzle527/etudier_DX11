인풋과 아웃풋이 맞물려있는 처리 단계

그래픽,렌더링 파이프라인

In(정점,)->          

VS(버텍스 쉐이더)Input:정점    
공간변환
Local(Model)-> W -> V -> P

RS(레스터 라이저)픽셀화

PS(픽셀 쉐이더)Input:픽셀      단위가 어마무시함

Out-> 최종그래픽(해상도)



디바이스 : 연결할 자원을 만드는 인터페이스

디바이스컨텍스트 : 자원을 바인딩하거나 파이프라인에 접근해주는 인터페이스

스왑체인: 그래픽하드웨어간 통신을 통해 제어해주는 인터페이스
-더블버퍼링 (화면 깜빡임)
-수직동기화 (화면 찢어짐)